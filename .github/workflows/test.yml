---
name: _test
description: Test trigger workflow

on:
  workflow_call:
    inputs:
      build_matrix:
        required: true
        type: string
        description: Build matrix for the test job
      build_urls:
        required: false
        type: string
        description: JSON map of image_name to build URL (optional compatibility input)
      
      # === Optional knobs for lava-test-plans ===
      distro_name:
        description: Default distro for lava-test-plans if not provided in matrix
        required: false
        type: string
        default: qcom-distro
      kernel:
        description: Extra path suffix for kernel (e.g., -6.18). Default empty.
        required: false
        type: string
        default: ""
      testplan:
        description: Test plan name to use in job generation (lava-test-plans)
        required: true
        type: string
        default: 'pre-merge'
      prefix:
        description: Prefix for the compressed artifact with generated jobs
        required: false
        type: string
        default: testjobs
      
      # === LAVA server configuration ===
      lava_url:
        description: LAVA server URL
        required: false
        type: string
        default: lava.infra.foundries.io
      wait_for_job:
        description: Wait for LAVA job to complete
        required: false
        type: boolean
        default: true
      fail_on_test_failure:
        description: Fail workflow if LAVA test fails
        required: false
        type: boolean
        default: true
    secrets:
      LAVATOKEN:
        required: true
jobs:
 submit_lava:
    runs-on:
      group: GHA-AudioReach-SelfHosted-RG
      labels: [ self-hosted, audior-prd-u2204-x64-large-od-ephem ]
    strategy:
      fail-fast: false
      matrix:
        build_matrix: ${{ fromJson(inputs.build_matrix) }}
    
    steps:
      - name: Sync Codebase
        id: sync
        uses: Audioreach/meta-audioreach/.github/actions/sync@ci
        with:
          event_name: ${{ github.event_name }}
          pr_ref:  ${{ github.event_name == 'pull_request' && github.event.pull_request.head.ref || '' }}
          pr_repo: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name || '' }}
          base_ref: ${{ github.ref_name }}

      - name: Get build URL
        id: get_build_url
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        env:
          BUILD_URLS_JSON: ${{ inputs.build_urls }}
          KAS_BASENAME: ${{ matrix.build_matrix.kas_basename }}
        with:
          script: |
            const urls = JSON.parse(process.env.BUILD_URLS_JSON || '{}')
            const image = process.env.KAS_BASENAME

            const key = `presigned_url_${image}.txt`
            const matched = urls[key]

            if (!matched) {
              throw new Error(`No exact build URL found for build variant: ${image}`)
            }

            console.log(`Using URL for ${image}: ${matched}`)
            core.setOutput('build_url', matched)

      - name: Print trigger info
        env:
          EVENT_NAME: ${{ github.event_name }}
          BUILD_URL: ${{ steps.get_build_url.outputs.build_url }}
          IMAGE_NAME: ${{ matrix.build_matrix.image_name }}
          MACHINE_NAME: ${{ matrix.build_matrix.machine_name }}
        run: |
          echo "Triggered by \"$EVENT_NAME\""
          echo "Build URL: \"$BUILD_URL\""
          echo "Image: \"$IMAGE_NAME\""
          echo "Machine: \"$MACHINE_NAME\""
      
      - name: Submit LAVA test job
        id: lava_submit
        uses: Audioreach/meta-audioreach/.github/actions/lava-test-plans@ci
        with:
          image_name: ${{ matrix.build_matrix.image_name }}
          machine_name: ${{ matrix.build_matrix.machine_name }}
          kas_basename: ${{ matrix.build_matrix.kas_basename }}
          build_url: ${{ steps.get_build_url.outputs.build_url }}
          distro_name: ${{ inputs.distro_name }}
          kernel: ${{ inputs.kernel }}
          testplan: ${{ inputs.testplan }}
          prefix: ${{ inputs.prefix }}
          lava_token: ${{ secrets.LAVATOKEN }}
          lava_url: ${{ inputs.lava_url }}
          wait_for_job: ${{ inputs.wait_for_job }}
          fail_action_on_failure: ${{ inputs.fail_on_test_failure }}
          run_id: ${{ github.run_id }}
          run_attempt: ${{ github.run_attempt }}
  
 publish-test-summary:
    name: "Publish Tests Summary"
    needs: [submit_lava]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    permissions:
      checks: write
      pull-requests: write
    outputs:
      summary_id: ${{ steps.generate-summary.outputs.artifact_id }}

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Generate Summary
        id: generate-summary
        uses: Audioreach/meta-audioreach/.github/actions/test-job-summary@ci
        with:
          build_id: ${{ github.run_id }}
          gh_token: ${{ secrets.GITHUB_TOKEN }}
          summary_file_name: test_job_summary
      
      # Download ALL matching summary artifacts from parallel jobs and put into one folder.
      - name: Download Summaries
        uses: actions/download-artifact@018cc2cf5baa6db3ef3c5f8a56943fffe632ef53 # v6
        with:
          pattern: test_job_summary*          # match all parallel summaries
          merge-multiple: true                # place all matched artifact contents into a single folder
          path: summaries

      - name: Publish Test Job Details
        run: |
          shopt -s nullglob
          files=(summaries/test_job_summary*)
          if [ ${#files[@]} -eq 0 ]; then
            echo ":warning: No test job summaries were found to publish." >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          echo "## LAVA Test Job Summaries" >> "$GITHUB_STEP_SUMMARY"
          for f in "${files[@]}"; do
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "### $(basename "$f")" >> "$GITHUB_STEP_SUMMARY"
            cat "$f" >> "$GITHUB_STEP_SUMMARY"
          done
